# Tasks: spec-engine-v3-enforcement

> Generated by spec-engine v2.2.0

<!-- Task lifecycle: pending -> in_progress -> completed
     Wired lifecycle: pending -> yes | n/a (for infra tasks with nothing to wire)
     Each task tracks: Status, Wave, Wired, Dependencies, Files
     Wave assignments are pre-computed by topological sort of dependency DAG.
     Tasks in the same wave have no mutual dependencies and can be batched.
     Files field lists which files this task will create or modify (enables parallel execution).
     Tasks in the same wave with non-overlapping Files can run in parallel.
     A task is only truly done when Status: completed AND Wired: yes (or n/a).
     Every implementation task MUST include at least one error-path acceptance criterion.
     IMPORTANT: Do NOT put wave numbers in section headers. The Wave: field is the sole source of truth. -->

## Phase 1: Setup

### T-1: Add `phase` and `acceptance` fields to state.json template

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-1 AC-3, US-1 AC-15, US-4 AC-4, US-13 AC-1, US-14 AC-4
- **Files**: templates/state.json
- **Wire into**: n/a
- **Description**: Modify `templates/state.json` to add two new top-level fields.

  **Change 1**: Add `"phase": null` after the `quick_mode` line (between `quick_mode` and `integrity`).

  **Change 2**: Add an `acceptance` object after the `security` object (at the very end, before the closing `}`):
  ```json
  "acceptance": {
    "status": null,
    "estimated_fix_rounds": null
  }
  ```

  The resulting field order must be: `version`, `spec_name`, `created_at`, `quick_mode`, `phase`, `integrity`, `tasks`, `waves`, `parallel`, `execution`, `reproducibility`, `quality_gates`, `lessons_applied`, `audit_log`, `security`, `acceptance`.

  Do not change any other field. The `phase` field value is `null` (not `"spec"`) in the template — new specs start at `null` until `/spec` sets it to `"spec"`.

- **Acceptance Criteria**:
  1. `templates/state.json` contains `"phase": null` as a top-level field positioned after `"quick_mode": false`
  2. `templates/state.json` contains an `"acceptance"` object with `"status": null` and `"estimated_fix_rounds": null` as the final top-level field
  3. The file remains valid JSON (all 16 top-level fields present, no syntax errors)
  4. If an agent reads a state.json created from this template and finds no `phase` field: the template was not applied correctly (error path — both fields must be present)

---

## Phase 2: Core Implementation

### T-2: Add types-first check and sweep enforcement to spec-validator

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-6 AC-1, US-6 AC-2, US-6 AC-3, US-9 AC-1, US-9 AC-2, US-9 AC-3
- **Files**: agents/spec-validator.md
- **Wire into**: n/a
- **Description**: Modify `agents/spec-validator.md` with three changes.

  **Change 1 — Add Types-First Wave 0 check**: Append a new "### 9. Types-First Wave 0" section after the existing Check 8 in the Validation Checklist section:
  ```markdown
  ### 9. Types-First Wave 0

  - [ ] IF tasks.md has 5 or more `### T-` headings, check that at least one task with
        `Wave: 0` has a `Files` entry or description referencing any of: `types`, `schemas`,
        `interfaces`, `contracts`, or `shared types`
  - [ ] IF 5+ tasks and no types task in Wave 0: report ERROR "Wave 0 has no types/schemas
        task. Specs with 5+ tasks require contract-first Wave 0."
  - [ ] IF fewer than 5 tasks: skip this check without reporting
  ```

  **Change 2 — Strengthen sweep enforcement in Check 7**: Find the existing Check 7 (Deprecated Field Detection) block and append two new checklist items to it:
  ```markdown
  - [ ] IF any task has `Deprecates` != `"none"` AND no sweep task exists whose `Dependencies`
        field includes that task ID: report ERROR "Task T-X has Deprecates field but no sweep
        task exists to clean up deprecated references"
  - [ ] IF a sweep task exists but its `Files` array does not cover all files containing
        deprecated field references (per grep at validation time): report ERROR "Sweep task
        T-Y Files array does not cover N file(s) containing deprecated field references"
  ```

  **Change 3 — Update Severity Rubric ERROR list**: Find the ERROR section in the Severity Rubric and add two entries:
  - `"Wave 0 missing types/schemas task in a spec with 5+ tasks"`
  - `"Deprecates-bearing task with no corresponding sweep task"`

- **Acceptance Criteria**:
  1. `agents/spec-validator.md` contains a "### 9. Types-First Wave 0" section with the three checklist items (5+ threshold, ERROR message, skip condition)
  2. Check 7 contains the two new ERROR-level sweep enforcement items referencing task ID lookup and file coverage
  3. The Severity Rubric ERROR list contains both new entries
  4. If a spec has exactly 4 tasks: reading the validator confirms the types-first check is skipped (error path — below the 5-task threshold)

---

### T-3: Add types-first enforcement to spec-tasker

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-6 AC-4
- **Files**: agents/spec-tasker.md
- **Wire into**: n/a
- **Description**: Modify `agents/spec-tasker.md` to add enforcement text to the existing "Contract-First Design Rule" section.

  Read the file first to locate the exact section name. Append the following block immediately before the closing of the Contract-First Design Rule section (or at the end of the section's content):
  ```markdown
  **Enforcement**: For specs with 5 or more tasks, placing a types/schemas task in Wave 0
  is required, not optional. The validator will report an ERROR if Wave 0 lacks a
  types/schemas task when total task count >= 5. Always create a Wave 0 task for shared
  types, interfaces, or contracts when the total task count is 5 or more.
  ```

- **Acceptance Criteria**:
  1. `agents/spec-tasker.md` Contract-First Design Rule section contains the enforcement text with the phrase "required, not optional"
  2. The enforcement text references both the 5-task threshold and the validator ERROR consequence
  3. If the Contract-First Design Rule section does not exist: do not create a section with a different name — this is a blocker; report it as a failure (error path)

---

### T-4: Add anti-deferral rule to spec-implementer, spec-tester, spec-debugger

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-7 AC-1, US-7 AC-2
- **Files**: agents/spec-implementer.md, agents/spec-tester.md, agents/spec-debugger.md
- **Wire into**: n/a
- **Description**: Add an "## Anti-Deferral Rule" section to each of the three agent files. The section content is identical for all three. Read each file first to find the appropriate insertion point (typically after the existing iron law or verification section).

  Add to each file:
  ```markdown
  ## Anti-Deferral Rule

  You MUST implement every acceptance criterion assigned to this task. Do not log warnings,
  add TODO comments, create stubs, or defer to future specs. If you cannot complete a
  criterion, report it as a failure with specific blockers -- never silently skip it.

  THE FOLLOWING PHRASES ARE PROHIBITED in your output unless accompanied by a FAILURE report:
  - "deferred to future spec"
  - "TODO: implement later"
  - "stub for now"
  - "placeholder implementation"
  ```

  Do not duplicate if a section named "Anti-Deferral Rule" already exists — instead verify it contains all 4 prohibited phrases and update if needed.

- **Acceptance Criteria**:
  1. Each of the 3 files (`agents/spec-implementer.md`, `agents/spec-tester.md`, `agents/spec-debugger.md`) contains an "## Anti-Deferral Rule" section with all 4 prohibited phrases listed
  2. The prohibited phrases are identical across all 3 files
  3. If any file already contains "Anti-Deferral Rule" but is missing one of the 4 phrases: update it (error path — must have all 4 phrases, not just the section)

---

### T-5: Add anti-deferral rule and fix budget estimation to spec-acceptor

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-7 AC-1, US-7 AC-2, US-14 AC-1, US-14 AC-3
- **Files**: agents/spec-acceptor.md
- **Wire into**: n/a
- **Description**: Modify `agents/spec-acceptor.md` with two changes. Read the file fully first.

  **Change 1 — Anti-Deferral Rule section**: Add the same "## Anti-Deferral Rule" section as T-4 (identical content including all 4 prohibited phrases). Find the appropriate insertion point by reading the file.

  **Change 2 — Fix Budget Estimation step**: Locate the Step 5 (Write Report) section. After the closing of Step 5's content, add a new "### Step 5.5: Estimate Fix Effort" section:
  ```markdown
  ### Step 5.5: Estimate Fix Effort

  After determining overall acceptance status:

  If overall status is NOT ACCEPTED:
  1. Count failed and partial criteria across all user stories
  2. Categorize gaps:
     - Wiring-only gaps (0 functional failures, only unwired tasks): `estimated_fix_rounds = 1`
     - Functional gaps in 3 or fewer criteria: `estimated_fix_rounds = 2`
     - Functional gaps in 4 to 6 criteria: `estimated_fix_rounds = 3`
     - Functional gaps in 7 or more criteria: `estimated_fix_rounds = 3 + ceil(gap_count / 3)`
  3. Include in the acceptance report: "Estimated fix effort: N iteration(s)"

  If overall status is ACCEPTED: `estimated_fix_rounds = 0`

  Always include the `estimated_fix_rounds` value in the report so the orchestrator
  can update `state.json.acceptance`.
  ```

- **Acceptance Criteria**:
  1. `agents/spec-acceptor.md` contains an "## Anti-Deferral Rule" section with all 4 prohibited phrases
  2. `agents/spec-acceptor.md` contains a "### Step 5.5: Estimate Fix Effort" section with the 4 categorization tiers and the `estimated_fix_rounds` output instruction
  3. The Step 5.5 section specifies `estimated_fix_rounds = 0` for the ACCEPTED case
  4. If Step 5 is not found: read the file and locate the actual final report-writing step, then insert after it (error path — do not guess step numbers)

---

### T-6: Add audit mode to spec-documenter

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-15 AC-1, US-15 AC-2, US-15 AC-3, US-15 AC-4
- **Files**: agents/spec-documenter.md
- **Wire into**: n/a
- **Description**: Add an "## Audit Mode" section to `agents/spec-documenter.md`. Read the file first.

  Find the appropriate insertion point (after the main Process or Documents to Generate section, before Quality Rules if present, or at the end of the file).

  Add:
  ```markdown
  ## Audit Mode

  When dispatched with audit-mode instructions (the dispatch message contains "audit" or
  the phrase "Do NOT generate documentation files"):

  1. Do NOT create or modify any files in `docs/` or other documentation directories
  2. Scan all implementation files listed in `state.json.tasks` (from each task's `Files` array)
  3. For each file, check:
     - Exported functions/classes without JSDoc or docstring comments
     - React components without prop type documentation
     - Public API methods without parameter/return documentation
  4. Compile results:
     - Count of undocumented exports
     - List of specific functions/components missing documentation
     - Documentation coverage percentage: `documented_count / total_count * 100`
  5. Write ONLY to `evidence/doc-audit.md` with this structure:
     ```
     # Documentation Audit
     Coverage: X% (N of M exports documented)
     Undocumented exports: [list]
     Undocumented functions: [list]
     ```
  6. Do not write to any other file. Return after writing evidence/doc-audit.md.
  ```

- **Acceptance Criteria**:
  1. `agents/spec-documenter.md` contains an "## Audit Mode" section with the 6-step process
  2. Step 1 explicitly states "Do NOT create or modify any files in `docs/`"
  3. Step 5 specifies writing only to `evidence/doc-audit.md` with the Coverage/Undocumented structure
  4. If the file does not have expected section headings: place Audit Mode at the end of the file without overwriting existing content (error path)

---

### T-7: Update skills/spec-exec/SKILL.md with all v3 enforcement changes

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-1, US-1 AC-2, US-1 AC-12, US-1 AC-15, US-1 AC-18, US-2 AC-1, US-2 AC-2, US-2 AC-3, US-2 AC-4, US-2 AC-5, US-2 AC-6, US-3 AC-1, US-3 AC-2, US-3 AC-3, US-3 AC-4, US-3 AC-5, US-3 AC-6, US-4 AC-1, US-4 AC-2, US-4 AC-3, US-4 AC-4, US-5 AC-1, US-5 AC-2, US-5 AC-5, US-10 AC-1, US-10 AC-2, US-10 AC-3, US-10 AC-4, US-11 AC-1, US-11 AC-2, US-11 AC-3, US-11 AC-4
- **Files**: skills/spec-exec/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-exec/SKILL.md` fully before making any changes. The verified step structure is: 1, 1.5 (Drift Detection), 2, 3, 4, 4.5, 5, 5.5, 5.6 (Wired Verification), 6, 7, 8. Make 8 changes in order:

  **Change 1 — Phase Gate**: Insert a new "## Phase Gate" section before the Workflow section (before Step 1):
  ```markdown
  ## Phase Gate

  Before proceeding, read `state.json.phase`. If the field is absent, treat as `"spec"`.

  **Required phase**: `"validated"`
  **Phase order**: spec(1) -> validated(2) -> executed(3) -> accepted/audited(4) -> documented(5) -> released(6) -> verified(7) -> retro(8)

  If `state.json.phase` has not reached the required phase (compare numeric order), display:
  "Phase gate: /spec-exec requires phase 'validated' to be complete. Current phase: '<CURRENT>'. Run /spec-validate first."
  Stop execution. Do not proceed to any subsequent step.
  Do NOT expose state.json field names, filesystem paths, or stack traces in this message.
  ```

  **Change 2 — Dependency Gate**: Insert a new "## Dependency Gate" section immediately after the Phase Gate section:
  ```markdown
  ## Dependency Gate

  Read `requirements.md` for a `## Depends On` section. For each listed dependency spec name:
  1. Validate the spec name contains only alphanumeric characters, hyphens, and underscores.
     If it contains path separators (`/`, `\`) or `..` sequences: log a warning and skip it.
  2. Read `.claude/specs/<dep-name>/state.json`
  3. Check `state.json.phase` is at phase order >= 4 (`"accepted"` or `"audited"` or later).
     If `phase` is absent, treat as not accepted.
  4. If any dependency is not accepted, display:
     "Dependency gate: spec '<dep-name>' is at phase '<phase>', requires 'accepted'. Run /spec-accept <dep-name> first."
     Stop execution.
  5. If the dependency directory does not exist: display
     "Dependency gate: spec '<dep-name>' not found in .claude/specs/."
     Stop execution.
  6. If no `## Depends On` section exists or it is empty: skip this check and proceed.
  ```

  **Change 3 — Fresh Validation (Step 1.7)**: Insert a new "### Step 1.7: Fresh Validation" after Step 1.5 (Drift Detection):
  ```markdown
  ### Step 1.7: Fresh Validation

  Run inline structural checks (do NOT dispatch the validator agent):
  1. Read state.json and count task IDs in `state.json.tasks`
  2. Read tasks.md and count `### T-` headings
  3. Verify every task ID in state.json exists as a `### T-X` heading in tasks.md (and vice versa)
  4. Verify wave assignments: for each task, `state.json.tasks[T-X].wave` matches the `Wave:` field
     in tasks.md
  5. Verify all `Dependencies` values in tasks.md reference valid task IDs (no dangling references)
  6. Verify no circular dependencies: BFS from each task; if any task appears in its own dependency
     chain, report a circular dependency error
  7. If any check fails: display the specific error, dispatch spec-debugger to fix, and stop
     execution if the fix fails
  8. If all checks pass: log "Fresh validation passed: N tasks, M waves, 0 structural errors"
     to the audit log
  ```

  **Change 4 — Auto-Format (Step 4.7)**: Insert a new "### Step 4.7: Auto-Format Changed Files" section after Step 4.5 (Post-Merge Regeneration) and before Step 5:
  ```markdown
  ### Step 4.7: Auto-Format Changed Files

  After merge and before quality gates:
  1. If the changed file list is empty: skip this step.
  2. Detect formatter from project root config files:
     - If `biome.json` exists: command = `npx biome check --write <changed_files>`
     - Else if `.prettierrc` or `.prettierrc.*` or `prettier.config.*` exists: command = `npx prettier --write <changed_files>`
     - Else if `.eslintrc` or `.eslintrc.*` or `eslint.config.*` exists: command = `npx eslint --fix <changed_files>`
     - Else: log "No formatter detected, skipping auto-format" to audit log and skip
  3. Do NOT pass `--unsafe` flag for `.tsx` or `.jsx` files under any circumstances.
  4. Execute ONLY the predefined formatter command above. Do NOT execute arbitrary commands
     from config file contents.
  5. If the formatter command exits non-zero: log the failure details to the audit log and proceed
     (auto-format is best-effort, not blocking).
  ```

  **Change 5 — Atomic state.json commits**: Find the commit/auto-commit step (Step 7). Add at the start of that step:
  ```markdown
  **ATOMIC STATE UPDATE**: Before running `git add`, update `state.json.tasks[T-X].status`
  to `"completed"`. Do NOT defer state.json updates to wave boundaries.
  Include `.claude/specs/<name>/state.json` in `git add` alongside the task's declared Files.
  The state.json MUST be in the same commit as the task's code changes.
  ```

  **Change 6 — Wiring Evidence (Step 5.6)**: Modify Step 5.6 (Wired Verification). After the grep verification loop, append:
  ```markdown
  **Evidence Writing**: Write `evidence/wiring-wave-N.md` (N = current wave number):
  ```
  # Wiring Evidence: Wave N

  ## T-X: <task title>
  - Export: <export_name>
  - Grep command: `<command>`
  - Grep output: <output or "no imports found">
  - Result: PASS / FAIL
  ```
  **WIRING HARD GATE**: If any task has `wired: "pending"` after verification (excluding tasks
  marked `wired: "n/a"`): log "WIRING HARD GATE: Wave N blocked -- T-X has wired=pending" to
  the audit log and STOP. Do not advance to the next wave.
  ```

  **Change 7 — Shared File Isolation**: Find Step 3 (Select Task Batch) or the pre-parallel setup. Add before building parallel groups:
  ```markdown
  **Shared File Hard Gate**: Before building parallel groups for this wave:
  1. Read `state.json.parallel.shared_files`
  2. For each task in the wave, compare its `Files` array against `shared_files`
  3. If ANY file matches: move that task to a sequential sub-batch
  4. Log "SHARED FILE ISOLATION: T-X moved to sequential -- owns shared file <path>"
  5. After the wave completes, run barrel reconciliation: for each file in `shared_files`
     modified by any task (detected via `git diff`), consolidate all modifications into a
     single coherent version. If reconciliation cannot auto-resolve a conflict, log it and
     dispatch spec-debugger.
  ```

  **Change 8 — Phase advancement (Step 8)**: Find Step 8 (Completion Check). Add to the branch where all tasks are complete:
  ```markdown
  When all tasks have `status: "completed"` and `wired: "yes"` or `"n/a"`:
  Set `state.json.phase` to `"executed"`.
  Log "Phase advanced to 'executed'" to the audit log.
  ```

- **Acceptance Criteria**:
  1. `skills/spec-exec/SKILL.md` contains "## Phase Gate" with required phase `"validated"` and the do-not-expose warning
  2. "## Dependency Gate" is present with spec name path separator validation (alphanumeric/hyphens/underscores only)
  3. Step 1.7 Fresh Validation is present with the 8-point BFS circular dependency check
  4. Step 4.7 Auto-Format is present with formatter detection and the prohibition on `--unsafe` for tsx/jsx
  5. Step 5.6 contains evidence writing to `evidence/wiring-wave-N.md` and the WIRING HARD GATE instruction
  6. Step 7 (commit) contains the ATOMIC STATE UPDATE instruction with state.json in git add
  7. Step 8 sets `state.json.phase` to `"executed"` when all tasks are complete and wired
  8. If step numbering differs from the documented structure: read the file fully before inserting at the correct logical locations (error path)

---

### T-8: Update skills/spec-loop/SKILL.md with all v3 enforcement changes

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-1, US-1 AC-2, US-1 AC-12, US-1 AC-15, US-1 AC-18, US-2 AC-1, US-2 AC-2, US-2 AC-3, US-2 AC-4, US-2 AC-5, US-2 AC-6, US-3 AC-1, US-3 AC-2, US-3 AC-3, US-3 AC-4, US-3 AC-5, US-3 AC-6, US-4 AC-1, US-4 AC-2, US-4 AC-3, US-4 AC-4, US-5 AC-1, US-5 AC-2, US-5 AC-5, US-10 AC-1, US-10 AC-2, US-10 AC-3, US-10 AC-4, US-11 AC-1, US-11 AC-2, US-11 AC-3, US-11 AC-4
- **Files**: skills/spec-loop/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-loop/SKILL.md` fully before making any changes. The verified step structure is: 1 (Initialize), 2 (Wave Loop with substeps 2a-2e), 3, 4. Make 8 changes (identical logic to T-7 adapted to spec-loop step numbering):

  **Change 1 — Phase Gate**: Insert "## Phase Gate" before "## Execution Loop" (or before Step 1). Same text as T-7 except replace `/spec-exec` with `/spec-loop`.

  **Change 2 — Dependency Gate**: Insert "## Dependency Gate" after Phase Gate. Identical content to T-7.

  **Change 3 — Fresh Validation**: Add to Step 1 (Initialize), after drift detection (Step 1, item 7). Add a "Fresh Validation" subsection with the same 8-point checklist as T-7's Step 1.7.

  **Change 4 — Auto-Format**: Add to Step 2d (after auto-commit in the wave loop): "After auto-commit, run auto-format on changed files." Add the same 5-point formatter detection block as T-7's Step 4.7.

  **Change 5 — Atomic state.json**: Modify Step 2d auto-commit to include: "Update `state.json.tasks[T-X].status` to `'completed'` BEFORE running git add. Include `.claude/specs/<name>/state.json` in the git add file list. state.json MUST be in the same commit."

  **Change 6 — Wiring Evidence**: Modify Step 2e.2 (wiring verification) to add evidence file writing to `evidence/wiring-wave-N.md` and the WIRING HARD GATE (same format as T-7's Step 5.6).

  **Change 7 — Shared File Isolation**: Modify Step 2b (Pre-Parallel Setup) to add the Shared File Hard Gate before building parallel groups. Same logic as T-7 including reconciliation and debugger dispatch on conflict.

  **Change 8 — Phase advancement**: Add to Step 4 (Completion): "When the loop exits with all tasks completed and wired: set `state.json.phase` to `'executed'`. Log 'Phase advanced to executed' to the audit log."

- **Acceptance Criteria**:
  1. `skills/spec-loop/SKILL.md` contains "## Phase Gate" with required phase `"validated"`
  2. "## Dependency Gate" is present with spec name path separator validation
  3. Step 1 contains inline Fresh Validation with the BFS circular dependency check
  4. Step 2d contains auto-format with formatter detection and tsx/jsx --unsafe prohibition
  5. Step 2d auto-commit instruction includes state.json in git add
  6. Step 2e.2 writes `evidence/wiring-wave-N.md` and has the WIRING HARD GATE
  7. Step 2b has the Shared File Hard Gate with barrel reconciliation
  8. Step 4 sets `state.json.phase` to `"executed"` on loop completion
  9. If step numbering differs: read the file fully first, then insert at correct logical locations (error path)

---

### T-9: Update skills/spec-team/SKILL.md with all v3 enforcement changes

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-1, US-1 AC-2, US-1 AC-12, US-1 AC-15, US-1 AC-18, US-2 AC-1, US-2 AC-2, US-2 AC-3, US-2 AC-4, US-2 AC-5, US-2 AC-6, US-3 AC-1, US-3 AC-2, US-3 AC-3, US-3 AC-4, US-3 AC-5, US-3 AC-6, US-4 AC-1, US-4 AC-2, US-4 AC-3, US-4 AC-4, US-5 AC-1, US-5 AC-2, US-5 AC-5, US-11 AC-1, US-11 AC-2, US-11 AC-3, US-11 AC-4
- **Files**: skills/spec-team/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-team/SKILL.md` fully before making any changes. The verified phase structure is: 0, 1, 2, 3, 4, 5. Make 7 changes (spec-team has no Fresh Validation — US-10 covers only exec and loop):

  **Change 1 — Phase Gate**: Insert "## Phase Gate" before "## Team Workflow" (or before Phase 0 if no separate heading). Same text as T-7 with `/spec-team` replacing `/spec-exec`.

  **Change 2 — Dependency Gate**: Insert "## Dependency Gate" after Phase Gate. Identical content to T-7 (path validation, 6 steps).

  **Change 3 — Auto-Format**: Add to Phase 1 (after auto-commit): "After auto-commit and before quality gates, run auto-format on changed files." Add the same 5-point formatter detection block as T-7.

  **Change 4 — Atomic state.json**: Modify Phase 5 (Commit) to include: "Update `state.json.tasks[T-X].status` to `'completed'` BEFORE running git add. Include `.claude/specs/<name>/state.json` in the same commit as the task's implementation files."

  **Change 5 — Wiring Evidence**: Modify Phase 4 (Post-Wave Verification) wiring check to write `evidence/wiring-wave-N.md` after verification and add the WIRING HARD GATE blocking wave advancement on pending tasks.

  **Change 6 — Shared File Isolation**: Modify Phase 0 (Pre-Wave Setup) to add the Shared File Hard Gate before building agent assignments. Same logic as T-7 including reconciliation.

  **Change 7 — Phase advancement**: Add to the "## Completion" section: "When all tasks are completed and wired: set `state.json.phase` to `'executed'`. Log 'Phase advanced to executed' to the audit log."

- **Acceptance Criteria**:
  1. `skills/spec-team/SKILL.md` contains "## Phase Gate" with required phase `"validated"`
  2. "## Dependency Gate" is present with spec name path separator validation
  3. Phase 1 contains auto-format step with formatter detection and tsx/jsx --unsafe prohibition
  4. Phase 5 includes state.json in git commit with the ATOMIC STATE UPDATE instruction
  5. Phase 4 writes `evidence/wiring-wave-N.md` and has the WIRING HARD GATE
  6. Phase 0 has Shared File Hard Gate with barrel reconciliation
  7. "## Completion" section sets `state.json.phase` to `"executed"`
  8. If phase structure differs from documented: read the file fully first (error path)

---

### T-10: Update skills/spec/SKILL.md with phase advancement and budget auto-calc

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-3, US-13 AC-1, US-13 AC-3, US-13 AC-4
- **Files**: skills/spec/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec/SKILL.md` fully. The verified step structure is: 1, 2, 3, 4, 5, 5.5, 5.6, 6, 6.5, 7, 8, 9, 10. Make two changes:

  **Change 1 — Budget Auto-Calculation (Step 7.5)**: Insert a new "### Step 7.5: Auto-Calculate Budget" section after Step 7 (Tasks Phase):
  ```markdown
  ### Step 7.5: Auto-Calculate Budget

  After the tasker completes and `state.json.tasks` is populated:
  1. Count the total number of tasks in `state.json.tasks` (call it `task_count`)
  2. If `state.json.execution.budget_cap` is null: set it to `task_count * 50000`
  3. Log "Budget auto-calculated: <cap> tokens (<task_count> tasks x 50000)" to the audit log
  4. If `state.json.execution.budget_cap` is already non-null: do NOT overwrite it
     (manually configured budgets take precedence)
  ```

  **Change 2 — Phase advancement (Step 10)**: In Step 10 (Summary), add at the end of the step:
  ```markdown
  Set `state.json.phase` to `"spec"` to record that spec creation is complete.
  Log "Phase set to 'spec'" to the audit log.
  ```

- **Acceptance Criteria**:
  1. `skills/spec/SKILL.md` contains "### Step 7.5: Auto-Calculate Budget" with the `task_count * 50000` formula and the null-check guard
  2. Step 7.5 explicitly states "do NOT overwrite" when budget_cap is non-null
  3. Step 10 (Summary) contains the instruction to set `state.json.phase` to `"spec"`
  4. If `budget_cap` is already 200000 (set manually): Step 7.5 skips the calculation (error path — manual budget must be preserved)

---

### T-11: Update skills/spec-validate/SKILL.md with phase gate and phase advancement

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-4, US-1 AC-17
- **Files**: skills/spec-validate/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-validate/SKILL.md` fully. The verified step structure is: 1, 1.5, 2, 3, 3.5, 4, 5, 6, 7. Make two changes:

  **Change 1 — Phase Gate**: Insert "## Phase Gate" before the Workflow section (before Step 1). Spec-validate's required phase is `"spec"`:
  ```markdown
  ## Phase Gate

  Before proceeding, read `state.json.phase`. If the field is absent, treat as `"spec"`.

  **Required phase**: `"spec"`
  **Phase order**: spec(1) -> validated(2) -> executed(3) -> accepted/audited(4) -> documented(5) -> released(6) -> verified(7) -> retro(8)

  If `state.json.phase` has not reached the required phase (compare numeric order), display:
  "Phase gate: /spec-validate requires phase 'spec' to be complete. Current phase: '<CURRENT>'. Run /spec first."
  Stop execution. Do not proceed to any subsequent step.
  Do NOT expose state.json field names, filesystem paths, or stack traces in this message.
  ```

  Note: Since a missing `phase` field is treated as `"spec"`, existing specs (before this change) always pass this gate. This satisfies US-1 AC-17.

  **Change 2 — Phase advancement (Step 7)**: Find Step 7 (the final validation step). Add to the "pass" completion branch:
  ```markdown
  If validation status is "pass": set `state.json.phase` to `"validated"`.
  Log "Phase advanced to 'validated'" to the audit log.
  ```
  Note: Phase is NOT advanced if validation produces ERROR or WARNING results without resolution.

- **Acceptance Criteria**:
  1. `skills/spec-validate/SKILL.md` contains "## Phase Gate" with required phase `"spec"` and the do-not-expose warning
  2. The Phase Gate section explicitly notes that a missing `phase` field is treated as `"spec"` (backward compatibility)
  3. Step 7 sets `state.json.phase` to `"validated"` only on "pass" status
  4. If validation produces errors: `state.json.phase` is NOT advanced to `"validated"` (error path)

---

### T-12: Add phase gates and phase advancement to 5 remaining pipeline skills

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-1 AC-1, US-1 AC-2, US-1 AC-5, US-1 AC-7, US-1 AC-8, US-1 AC-9, US-1 AC-10, US-1 AC-11, US-1 AC-14, US-1 AC-15, US-1 AC-16, US-1 AC-18
- **Files**: skills/spec-security-audit/SKILL.md, skills/spec-docs/SKILL.md, skills/spec-release/SKILL.md, skills/spec-verify/SKILL.md, skills/spec-retro/SKILL.md
- **Wire into**: n/a
- **Description**: Add Phase Gate and phase advancement to each of the 5 remaining pipeline skills. Read each file before editing. The Phase Gate section uses the shared template parameterized per skill:

  **skills/spec-security-audit/SKILL.md**:
  - Required phase: `"executed"` (phase order 3)
  - Error message: "Phase gate: /spec-security-audit requires phase 'executed' to be complete. Current phase: '<CURRENT>'. Run /spec-exec (or /spec-loop or /spec-team) first."
  - Note in gate: "Both `/spec-accept` and `/spec-security-audit` require `'executed'` and may run in either order."
  - Phase advancement: When `evidence/security-audit.json` is written, set `state.json.phase` to `"audited"`

  **skills/spec-docs/SKILL.md**:
  - Required phase: `"accepted"` (phase order >= 4)
  - Error message: "Phase gate: /spec-docs requires phase 'accepted' to be complete. Current phase: '<CURRENT>'. Run /spec-accept first."
  - Note in gate: "Phase `'audited'` (order 4) also satisfies this requirement."
  - Phase advancement: On completion, set `state.json.phase` to `"documented"`

  **skills/spec-release/SKILL.md**:
  - Required phase: `"accepted"` (phase order >= 4)
  - Error message: "Phase gate: /spec-release requires phase 'accepted' to be complete. Current phase: '<CURRENT>'. Run /spec-accept first."
  - Note in gate: "Phase `'audited'` or `'documented'` also satisfy this requirement. The existing security gate check (CRITICAL findings) is a separate guard within the workflow."
  - Phase advancement: When release.md is written, set `state.json.phase` to `"released"`

  **skills/spec-verify/SKILL.md**:
  - Required phase: `"released"` (phase order 6)
  - Error message: "Phase gate: /spec-verify requires phase 'released' to be complete. Current phase: '<CURRENT>'. Run /spec-release first."
  - Phase advancement: When all smoke tests pass, set `state.json.phase` to `"verified"`

  **skills/spec-retro/SKILL.md**:
  - Required phase: `"verified"` (or `"released"` — phase order >= 6)
  - Error message: "Phase gate: /spec-retro requires phase 'verified' to be complete. Current phase: '<CURRENT>'. Run /spec-verify first."
  - Note in gate: "Phase `'released'` (order 6) also satisfies this requirement."
  - Phase advancement: When retro.md and lessons.json are written, set `state.json.phase` to `"retro"`

  For all 5 files: insert the Phase Gate section before the Workflow section. The do-not-expose warning must be present in each. Phase advancement goes at the final step of each workflow.

- **Acceptance Criteria**:
  1. Each of the 5 files contains a "## Phase Gate" section with the correct required phase and the do-not-expose warning
  2. Each file's final step sets `state.json.phase` to the correct value (audited/documented/released/verified/retro)
  3. `skills/spec-security-audit/SKILL.md` phase gate notes that accept and security-audit may run in either order from `"executed"`
  4. `skills/spec-retro/SKILL.md` phase gate allows entry from phase order >= 6 (both `"released"` and `"verified"`)
  5. `skills/spec-docs/SKILL.md` phase gate allows entry from phase order >= 4 (both `"accepted"` and `"audited"`)
  6. If a file lacks a "## Workflow" heading: insert Phase Gate before the first numbered step (error path)

---

### T-13: Update skills/spec-status/SKILL.md with budget source display

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-13 AC-2
- **Files**: skills/spec-status/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-status/SKILL.md` fully. Locate the Cost display section (shows tokens used and budget cap).

  Modify the budget cap display to show the budget source. Add the following display logic block:
  ```markdown
  **Budget display logic**:
  - Read `state.json.execution.budget_cap` (call it `cap`) and count tasks in `state.json.tasks`
    (call it `task_count`)
  - If `cap` is null: source = "(not set)"
  - Else if `cap == task_count * 50000`: source = "(auto-calculated, <task_count> tasks x 50000)"
  - Else: source = "(manual)"

  **Display format**:
  ```
  Tokens used: <total_tokens>
  Budget cap:  <cap> <source>
  ```
  ```

- **Acceptance Criteria**:
  1. `skills/spec-status/SKILL.md` Cost section contains the three-way budget source logic: auto-calculated, manual, not-set
  2. The auto-calculated display includes the formula annotation "(auto-calculated, N tasks x 50000)"
  3. If `budget_cap` is null: display shows "(not set)" without error (error path — null must be handled gracefully)

---

## Phase 3: Integration

### T-14: Update skills/spec-accept/SKILL.md with all acceptance-phase v3 changes

- **Status**: pending
- **Wave**: 2
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-5, T-7, T-8, T-9
- **Covers**: US-1 AC-6, US-1 AC-13, US-1 AC-15, US-1 AC-18, US-5 AC-3, US-5 AC-4, US-8 AC-1, US-8 AC-2, US-8 AC-3, US-8 AC-4, US-8 AC-5, US-8 AC-6, US-12 AC-1, US-12 AC-2, US-12 AC-3, US-12 AC-4, US-12 AC-5, US-12 AC-6, US-12 AC-7, US-14 AC-2, US-14 AC-3, US-14 AC-4, US-15 AC-1, US-15 AC-2, US-15 AC-3, US-15 AC-4
- **Files**: skills/spec-accept/SKILL.md
- **Wire into**: n/a
- **Description**: Read `skills/spec-accept/SKILL.md` fully. The verified structure has 5 workflow steps. Make 7 changes in order:

  **Change 1 — Phase Gate**: Insert "## Phase Gate" before the Workflow section. Required phase: `"executed"`:
  ```markdown
  ## Phase Gate

  Before proceeding, read `state.json.phase`. If the field is absent, treat as `"spec"`.

  **Required phase**: `"executed"`
  **Phase order**: spec(1) -> validated(2) -> executed(3) -> accepted/audited(4) -> documented(5) -> released(6) -> verified(7) -> retro(8)

  If `state.json.phase` has not reached the required phase (compare numeric order), display:
  "Phase gate: /spec-accept requires phase 'executed' to be complete. Current phase: '<CURRENT>'. Run /spec-exec (or /spec-loop or /spec-team) first."
  Stop execution. Do not proceed to any subsequent step.
  Do NOT expose state.json field names, filesystem paths, or stack traces in this message.
  Note: Both `/spec-accept` and `/spec-security-audit` require `'executed'` and may run in either order.
  ```

  **Change 2 — Pre-Acceptance Full Lint (Step 1.5)**: Insert before the wiring audit step:
  ```markdown
  ### Step 1.5: Pre-Acceptance Full Lint

  1. Read `state.json.quality_gates.lint_cmd`. If null or empty: log "Pre-acceptance lint
     skipped: no lint command configured" to audit log and skip to Step 1.6.
  2. Validate `lint_cmd` against `state.json.quality_gates.allowed_commands`. If
     `allowed_commands` is empty or `lint_cmd` is not in the list: log "Pre-acceptance lint
     skipped: lint_cmd not in allowed_commands" and skip to Step 1.6.
  3. Run `lint_cmd` on the full codebase (no file filtering).
  4. Write output to `evidence/pre-acceptance-lint.txt`:
     - Zero errors: write "PASS: 0 lint errors"
     - Errors present: write full lint output
  5. If `lint_cmd` fails to execute (command not found, crash — not lint errors): log the error
     to the audit log, write the error to `evidence/pre-acceptance-lint.txt`, and proceed.
  6. Include lint results in the acceptor's input.
  ```

  **Change 3 — Pre-Acceptance Security Scan (Step 1.6)**: Insert after Step 1.5:
  ```markdown
  ### Step 1.6: Pre-Acceptance Security Scan

  1. Read `state.json.reproducibility.git_sha_start`.
  2. If non-null: get changed files via `git diff --name-only <sha>..HEAD`
  3. If null: scope to all source files, excluding: `node_modules/`, `dist/`, `.next/`,
     `vendor/`, `.git/`
  4. Grep for patterns across scoped files:
     - Hardcoded secrets: `password\s*=\s*["']`, `api_key\s*=\s*["']`, `secret\s*=\s*["']`
     - SQL injection: `query\s*\+`, `"SELECT.*"\s*\+`
     - XSS sinks: `dangerouslySetInnerHTML`, `innerHTML\s*=`
     - Insecure crypto: `Math\.random\(\)` in security context, `md5\(`, `sha1\(`
  5. Write results to `evidence/pre-acceptance-security-scan.txt`:
     - Zero matches: write "PASS: No common vulnerability patterns detected"
     - Matches found: write ONLY file path, line number, and pattern name.
       Do NOT write matched line content. Do NOT write credential or secret values.
       Truncate any surrounding context to 200 characters maximum.
  6. Include in acceptor's input. Non-blocking: if grep fails, log the error and continue.
  ```

  **Change 4 — Documentation Audit (Step 1.7)**: Insert after Step 1.6:
  ```markdown
  ### Step 1.7: Documentation Audit

  1. Dispatch spec-documenter agent with these exact instructions:
     "Scan the implementation for documentation gaps. Do NOT generate documentation files.
      Report: undocumented exports, functions missing JSDoc/docstrings, components missing
      prop documentation. Write your findings ONLY to evidence/doc-audit.md."
  2. If the agent fails or times out: log the error to the audit log and proceed (non-blocking).
  3. Include `evidence/doc-audit.md` in the acceptor's input as non-blocking context.
  ```

  **Change 5 — Wiring Evidence Consumption**: Find the existing wiring audit step (step 2). Add at the end of that step:
  ```markdown
  Additionally, read all `evidence/wiring-wave-*.md` files and include their contents in the
  acceptor's input. For each completed wave in `state.json.waves`, verify a corresponding
  `evidence/wiring-wave-N.md` file exists. If any completed wave is missing its evidence file,
  flag: "WIRING EVIDENCE GAP: Wave N has no wiring evidence" in the acceptance input.
  ```

  **Change 6 — Fix budget state update**: After the acceptor writes acceptance.md, add a new step:
  ```markdown
  ### Step 5.5: Update state.json Acceptance Data

  After the acceptor completes and writes acceptance.md:
  1. Parse `estimated_fix_rounds` from the acceptance report (the "Estimated fix effort: N" line)
  2. Determine status: "accepted" if user confirmed, "not_accepted" otherwise
  3. Update `state.json.acceptance`:
     ```json
     {
       "status": "accepted" | "not_accepted",
       "estimated_fix_rounds": N
     }
     ```
  4. Write the updated state.json to disk.
  ```

  **Change 7 — Phase advancement**: Add to the final step (after user confirms acceptance):
  ```markdown
  When the user responds with acceptance ("Accept this implementation" or equivalent positive
  confirmation):
  Set `state.json.phase` to `"accepted"`.
  Log "Phase advanced to 'accepted'" to the audit log.
  ```

- **Acceptance Criteria**:
  1. `skills/spec-accept/SKILL.md` contains "## Phase Gate" with required phase `"executed"` and the both-skills-may-run note
  2. Step 1.5 contains `allowed_commands` validation and skips (not just warns) when `allowed_commands` is empty (US-8 AC-6)
  3. Step 1.6 writes ONLY file path, line number, and pattern name — not matched content; truncation to 200 characters is explicitly specified (US-12 AC-6, AC-7)
  4. Step 1.7 dispatches spec-documenter with the exact phrase "Do NOT generate documentation files"
  5. Step 2 (wiring audit) reads `evidence/wiring-wave-*.md` files and flags gaps with "WIRING EVIDENCE GAP"
  6. Step 5.5 updates `state.json.acceptance` with both status and estimated_fix_rounds
  7. Phase advancement to `"accepted"` is tied to user confirmation (not acceptor agent completion alone)
  8. If `state.json` lacks the `acceptance` field: Step 5.5 creates it (per US-14 AC-4) (error path)

---
